import type {Request, Response} from "express";

export default class StoreController {
    public async getStoreInfo(req: Request, res: Response) {}
    public async updateStoreInfo(req: Request, res: Response) {} 
}